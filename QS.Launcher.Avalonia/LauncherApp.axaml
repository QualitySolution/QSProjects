<Application x:Class="QS.Launcher.LauncherApp"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters1="clr-namespace:QS.Project.Converters;assembly=QS.Project.Avalonia"
             x:CompileBindings="False"
             RequestedThemeVariant="Light">
    <!--  "Default" ThemeVariant follows system theme variant. "Dark" or "Light" are other available options.  -->

    <Application.Resources>
        <converters1:NumericIsNotZeroConverter x:Key="NumericIsNotZeroConverter" />
        <converters1:ByteArrayToBitmapConverter x:Key="ByteArrayToBitmapConverter" />
    </Application.Resources>

    <Application.Styles>
        <FluentTheme />

        <Style Selector=":is(Control).animated">
            <Setter Property="Transitions">
                <Setter.Value>
                    <Transitions>
                        <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.15" />
                        <DoubleTransition Property="Opacity" Duration="0:0:0.15" />
                        <DoubleTransition Property="Height" Duration="0:0:0.15" />
                    </Transitions>
                </Setter.Value>
            </Setter>
        </Style>

        <Style Selector="Button">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
    </Application.Styles>

</Application>

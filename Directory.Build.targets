<Project>
  <Target Name="RemoveIncompatibleSystemReactiveAnalyzer"
          BeforeTargets="CoreCompile"
          Condition="'$(MSBuildRuntimeType)' == 'Mono'">
    <ItemGroup>
      <Analyzer Remove="@(Analyzer)" Condition="'%(Filename)%(Extension)' == 'System.Reactive.Analyzers.dll'" />
    </ItemGroup>
  </Target>
</Project>
